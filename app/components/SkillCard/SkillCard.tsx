import { FC, ReactElement } from "react";

interface SkillCardProps {}

const SkillCard: FC<SkillCardProps> = (): ReactElement => {
  return (
    <div>
      <h1>SkillCard</h1>
    </div>
  );
};

export default SkillCard;
